#ifndef MOD_AFTERWORLD_VEHICLES_SHIP
#define MOD_AFTERWORLD_VEHICLES_SHIP

#include "./common.h"

struct VehicleShip {};

void onVehicleFrameShip(objid id, VehicleState& state, VehicleShip& vehicleShip, ControlParams& controlParams);

#endif
